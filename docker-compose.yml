version: '3.8'
services:
  prometheus:
    build: .
    container_name: diy7111-ea2-carlos-campos-container
    ports:
      - '9090:9090'
    volumes:
      - prometheus_data:/prometheus
volumes:
  prometheus_data: